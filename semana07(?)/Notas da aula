# com while:
valor = 0 
while(valor < 10):
    print("-", valor)
    valor = valor + 1

# com for (ninguém mexe na variável, é usado para programas que fazem sequência de números ou coisas com limites já definidos. 
# Pirmeiro valor é o inicializador, segundo termina um número antes.
for valor in range(0,10):
    print("-", valor)


#fatorial é x números até 1
fatorial = int(input("Digite um valor:"))
resultado = 1
for x in range (1, fatorial +1):
    resultado = resultado * x 
print("Resultado: ",resultado)

#também dá pra fazer assim:
fatorial = int(input("Digite um valor:"))
resultado = fatorial
for x in range (1, fatorial):
    resultado = resultado * x 
print("Resultado do Feldmann: ",resultado)

#exercício
primeiro = int(input("Primeiro número:"))
ultimo = int(input("Segundo numero:"))
while (primeiro > ultimo):
    primeiro = int(input("Primeiro número:"))
    ultimo = int(input("Segundo numero:"))

media = 0
contador = 0
for numero in range (primeiro +1, ultimo):
    media = media + numero
    contador += 1

media = media / contador 
print("Média dos valores:", media)

#exercício
primeiro = int(input("Primeiro número:"))
ultimo = int(input("Segundo numero:"))

#ideia feldmann
while primeiro > ultimo:
    print("Erro: O primeiro valor deve ser menor que o segundo.")
    primeiro = int(input("Primeiro número:"))
    ultimo = int(input("Segundo numero:"))

##ideia roberto
if primeiro > ultimo:
    aux = primeiro 
    primeiro = ultimo 
    ultimo = aux
